[workspace]
resolver = "2"
members = ["crates/slimg-core", "crates/slimg-ffi", "crates/libjxl-sys", "cli"]
exclude = ["gui/src-tauri", "mig/src-tauri"]

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
